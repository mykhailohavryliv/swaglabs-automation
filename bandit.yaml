# Bandit YAML configuration
# See: https://bandit.readthedocs.io/en/latest/config.html

# Directories/files to skip during scan (YAML only).
exclude_dirs:
  - venv
  - .git
  - __pycache__
  - data

# Optional: include only these source folders (if you want to restrict targets)
# targets:
#   - pages
#  - tests
#  - utils
#  - config

# skips:
#   - B101
#   - B105